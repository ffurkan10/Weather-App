import React from "react";

const Error = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        maxWidth: "1300px",
        margin: "0 auto",
      }}
    >
      <h1 style={{ color: "white" }}>Could not get location.</h1>
    </div>
  );
};

export default Error;
